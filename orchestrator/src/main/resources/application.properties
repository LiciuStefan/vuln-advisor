spring.application.name=orchestrator

# Baza de date proprie (advisor_orchestrator)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/advisor_orchestrator
spring.datasource.username=${DB_USER:advisor}
spring.datasource.password=${DB_PASSWORD:advisor}

# RabbitMQ
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=5672
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
